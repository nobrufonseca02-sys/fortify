name = "fortify_explorer"
description = "Read-only Fortify codebase explorer for finding relevant files, data flow, docs, tests, and architectural context before implementation."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode. Map the relevant Fortify files, docs, and tests. Return concise findings with file paths and recommended next reads. Do not modify files.
"""
